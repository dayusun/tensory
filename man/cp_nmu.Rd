% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cp_variants.R
\name{cp_nmu}
\alias{cp_nmu}
\title{Nonnegative CP Decomposition via Multiplicative Updates}
\usage{
cp_nmu(X, R, tol = 1e-04, maxiters = 100L, init = "random", printitn = 0L)
}
\arguments{
\item{X}{A nonnegative Tensor or array-like object.}

\item{R}{Target CP rank.}

\item{tol}{Convergence tolerance on change in fit (default \code{1e-4}).}

\item{maxiters}{Maximum number of sweeps (default \code{100}).}

\item{init}{\code{"random"} (uniform) or a list of nonnegative factor matrices.}

\item{printitn}{Print fit every \code{printitn} iterations (\code{0} to suppress).}
}
\value{
A \code{KTensor} with nonnegative factors.
}
\description{
Computes a nonnegative CP decomposition with Lee-Seung style multiplicative
updates, mirroring the MATLAB Tensor Toolbox \code{cp_nmu}. All entries of \code{X}
must be nonnegative; factors stay nonnegative throughout.
}
\examples{
set.seed(1)
X <- tensor(array(runif(24), dim = c(2, 3, 4)))
K <- cp_nmu(X, R = 2, maxiters = 20)
}

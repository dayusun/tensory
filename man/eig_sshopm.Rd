% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensor_eigen.R
\name{eig_sshopm}
\alias{eig_sshopm}
\title{Shifted Symmetric Higher-Order Power Method (SS-HOPM)}
\usage{
eig_sshopm(
  A,
  shift = "adaptive",
  maximize = TRUE,
  start = NULL,
  tol = 1e-10,
  maxiters = 500L,
  tau = 1e-06
)
}
\arguments{
\item{A}{A symmetric Tensor (all modes the same size).}

\item{shift}{\code{"adaptive"} (default) or a fixed numeric shift.}

\item{maximize}{Logical; \code{TRUE} (default) seeks local maxima of \verb{A x^m}
(\code{beta = 1}), \code{FALSE} seeks local minima (\code{beta = -1}).}

\item{start}{Optional starting vector; random normal if omitted.}

\item{tol}{Convergence tolerance on the eigenvalue change (default
\code{1e-10}).}

\item{maxiters}{Maximum iterations (default \code{500}).}

\item{tau}{Positive-definiteness threshold for the adaptive shift
(default \code{1e-6}).}
}
\value{
A list with elements \code{lambda}, \code{x}, \code{converged}, \code{iterations}, and
\code{lambda_trace}.
}
\description{
Computes a Z-eigenpair \verb{A x^(m-1) = lambda x}, \verb{||x|| = 1}, of a symmetric
tensor by the shifted symmetric higher-order power method with the adaptive
shift of Kolda & Mayo, mirroring the MATLAB Tensor Toolbox \code{eig_sshopm}.
}
\examples{
set.seed(1)
A <- symmetrize(tensor(array(rnorm(81), dim = c(3, 3, 3, 3))))
res <- eig_sshopm(A)
}
\references{
Kolda, T. G. and Mayo, J. R. (2011). Shifted power method for
computing tensor eigenpairs. SIAM J. Matrix Anal. Appl. 32(4);
Kolda, T. G. and Mayo, J. R. (2014). An adaptive shifted power method for
computing generalized tensor eigenpairs. SIAM J. Matrix Anal. Appl. 35(4).
}

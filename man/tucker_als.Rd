% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tucker_decomposition.R
\name{tucker_als}
\alias{tucker_als}
\title{Tucker Alternating Least Squares (HOOI)}
\usage{
tucker_als(
  X,
  ranks,
  tol = 1e-04,
  maxiters = 50L,
  dimorder = NULL,
  init = "nvecs",
  printitn = 0L
)
}
\arguments{
\item{X}{A Tensor or array-like object.}

\item{ranks}{Integer vector of per-mode target ranks, or a scalar replicated
across modes.}

\item{tol}{Convergence tolerance on change in fit (default \code{1e-4}).}

\item{maxiters}{Maximum number of HOOI sweeps (default \code{50}).}

\item{dimorder}{Integer permutation giving the order in which factor
matrices are updated.}

\item{init}{Either \code{"nvecs"} (leading left singular vectors, default),
\code{"random"} (random orthonormal), or a list of initial factor matrices.}

\item{printitn}{Print fit every \code{printitn} iterations (\code{0} to suppress).}
}
\value{
A \code{TTensor}.
}
\description{
Computes a Tucker decomposition of a dense tensor with target multilinear
ranks via Higher-Order Orthogonal Iteration. Mirrors the MATLAB Tensor
Toolbox \code{tucker_als}.
}
\examples{
set.seed(1)
X <- tensor(array(runif(60), dim = c(3, 4, 5)))
T <- tucker_als(X, ranks = c(2, 3, 3), maxiters = 20)
}

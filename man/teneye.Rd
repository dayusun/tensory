% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensor_constructors.R
\name{teneye}
\alias{teneye}
\title{Identity Tensor}
\usage{
teneye(m, n)
}
\arguments{
\item{m}{Tensor order (even positive integer).}

\item{n}{Size of each mode.}
}
\value{
A symmetric \code{Tensor} of order \code{m} and size \code{n}.
}
\description{
Creates the order-\code{m} identity tensor \code{E} of size \code{n}, satisfying
\code{ttsv(E, x, -1) == x} for any vector \code{x} with \code{norm(x) == 1}. Mirrors the
MATLAB Tensor Toolbox \code{teneye}; as there, \code{m} must be even.
}
\examples{
E <- teneye(4, 3)
x <- rnorm(3); x <- x / sqrt(sum(x^2))
max(abs(ttsv(E, x, -1) - x)) < 1e-12
}

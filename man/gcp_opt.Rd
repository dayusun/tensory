% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gcp_opt.R
\name{gcp_opt}
\alias{gcp_opt}
\title{Generalized CP Decomposition}
\usage{
gcp_opt(
  X,
  R,
  type = "gaussian",
  init = "random",
  maxiters = 500L,
  factr = 1e+07,
  printitn = 0L
)
}
\arguments{
\item{X}{A Tensor or array-like object.}

\item{R}{Target CP rank.}

\item{type}{Loss name: one of \code{"gaussian"} (alias \code{"normal"}),
\code{"poisson"} (alias \code{"count"}), \code{"poisson-log"}, \code{"bernoulli-odds"},
\code{"bernoulli-logit"}, \code{"rayleigh"}, \code{"gamma"}, \code{"huber"}. Alternatively a
list with elements \code{f(x, m)}, \code{g(x, m)} (the derivative in \code{m}), and
\code{lower} (factor lower bound, \code{-Inf} if unconstrained) for a custom loss.}

\item{init}{\code{"random"}, \code{"nvecs"}, or a list of initial factor matrices.}

\item{maxiters}{Maximum optimizer iterations (default \code{500}).}

\item{factr}{\code{optim} L-BFGS-B \code{factr} convergence parameter.}

\item{printitn}{If positive, print the optimizer trace.}
}
\value{
A list with elements \code{K} (the fitted \code{KTensor}) and \code{objective}
(the final loss value).
}
\description{
Fits a CP model by minimizing the sum of an arbitrary elementwise loss
\code{f(x, m)} between the data and the model with L-BFGS-B, mirroring the
dense/deterministic mode of the MATLAB Tensor Toolbox \code{gcp_opt}.
}
\examples{
set.seed(1)
X <- tensor(array(rpois(24, 3), dim = c(2, 3, 4)))
res <- gcp_opt(X, R = 2, type = "poisson", maxiters = 100)
}
\references{
Hong, D., Kolda, T. G., and Duersch, J. A. (2020). Generalized
canonical polyadic tensor decomposition. SIAM Review 62(1).
}

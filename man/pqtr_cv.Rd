% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pqtr.R
\name{pqtr_cv}
\alias{pqtr_cv}
\title{Choose the Reduced Dimension of a Quantile Tensor Regression}
\usage{
pqtr_cv(
  X,
  y,
  tau = 0.5,
  Z = NULL,
  u_grid = NULL,
  nfolds = 5L,
  ridge = 1e-08,
  maxit = 200L,
  tol = 1e-08
)
}
\arguments{
\item{X, y, tau, Z, ridge, maxit, tol}{As in \code{\link[=pqtr]{pqtr()}}.}

\item{u_grid}{Optional list of candidate dimension vectors, each of length
\code{m} (a plain integer vector is read as one candidate per element, used in
every mode). Leave \code{NULL} (default) for the automatic equal-dimension
grid.}

\item{nfolds}{Number of cross-validation folds (default \code{5}).}
}
\value{
The \code{pqtr} fit at the selected dimension, with three extra elements:
\code{u_grid} (the candidates tried), \code{cv} (a data frame of \code{u} and mean
out-of-fold check \code{loss}), and \code{u_min} (the chosen dimension vector).
}
\description{
Runs \code{\link[=pqtr]{pqtr()}} over a grid of candidate reduced dimensions, scores each one
by \code{nfolds}-fold cross-validated check loss, and returns the model refitted
on all subjects at the best value. Use this when the eigenvalue-ratio rule
built into \code{\link[=pqtr]{pqtr()}} is not obviously right, for instance when the
coefficient array is not close to rank one.
}
\details{
The default grid keeps the same number of directions in every mode,
\code{u = rep(d, m)} for \code{d} from 1 up to the largest value for which the reduced
quantile regression stays estimable, capped at 12 as in the reference
implementation. That is a deliberate simplification: enumerating every
combination of per-mode dimensions costs \code{d^m} fits. Pass \code{u_grid}
explicitly, for instance
\code{u_grid = apply(expand.grid(1:2, 1:3), 1, as.integer, simplify = FALSE)},
when the modes should be allowed to differ.
}
\examples{
set.seed(2)
B <- outer(c(2, rep(0, 5)), c(2, rep(0, 4)))
X <- lapply(1:120, function(i) matrix(rnorm(30), 6, 5))
y <- vapply(X, function(xi) sum(B * xi), numeric(1)) + rnorm(120)

fit <- pqtr_cv(X, y, tau = 0.5, nfolds = 3)
fit$u_min
fit$cv
}
\seealso{
\code{\link[=pqtr]{pqtr()}}
}

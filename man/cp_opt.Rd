% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cp_variants.R
\name{cp_opt}
\alias{cp_opt}
\title{CP Decomposition via Direct Optimization}
\usage{
cp_opt(
  X,
  R,
  init = "random",
  lower = -Inf,
  maxiters = 500L,
  factr = 1e+07,
  printitn = 0L
)
}
\arguments{
\item{X}{A Tensor or array-like object.}

\item{R}{Target CP rank.}

\item{init}{\code{"random"} (scaled normal) or a list of initial factor
matrices.}

\item{lower}{Optional lower bound for all factor entries (e.g. \code{0} for a
nonnegative model); default unbounded.}

\item{maxiters}{Maximum optimizer iterations (default \code{500}).}

\item{factr}{\code{optim} L-BFGS-B \code{factr} convergence parameter.}

\item{printitn}{If positive, print the optimizer trace.}
}
\value{
A \code{KTensor}.
}
\description{
Fits a CP model by minimizing \verb{||X - K||^2} over all factor matrices
simultaneously with L-BFGS-B, mirroring the MATLAB Tensor Toolbox \code{cp_opt}.
}
\examples{
set.seed(1)
X <- tensor(array(rnorm(24), dim = c(2, 3, 4)))
K <- cp_opt(X, R = 2)
}

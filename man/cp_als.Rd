% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cp_decomposition.R
\name{cp_als}
\alias{cp_als}
\title{CP Alternating Least Squares Decomposition}
\usage{
cp_als(
  X,
  R,
  tol = 1e-04,
  maxiters = 50L,
  dimorder = NULL,
  init = "random",
  printitn = 0L,
  fixsigns = TRUE
)
}
\arguments{
\item{X}{A Tensor or array-like object.}

\item{R}{Target CP rank (positive integer).}

\item{tol}{Convergence tolerance on change in fit (default \code{1e-4}).}

\item{maxiters}{Maximum number of ALS sweeps (default \code{50}).}

\item{dimorder}{Integer permutation of \code{1:ndims(X)} giving the order in which
factor matrices are updated. Defaults to \code{1:ndims(X)}.}

\item{init}{Either \code{"random"} (i.i.d. normal), \code{"nvecs"} (leading left
singular vectors of the mode-n unfolding; falls back to random for modes
where the mode size is smaller than \code{R}), or a list of initial factor
matrices.}

\item{printitn}{Print fit every \code{printitn} iterations (\code{0} to suppress).}

\item{fixsigns}{Logical; if \code{TRUE}, resolve sign ambiguity of the returned
components.}
}
\value{
A \code{KTensor} giving the rank-R CP decomposition of \code{X}.
}
\description{
Computes a rank-R canonical polyadic (CP) decomposition of a dense tensor by
alternating least squares, mirroring the behavior of the MATLAB Tensor
Toolbox \code{cp_als}.
}
\examples{
set.seed(1)
X <- tensor(array(runif(24), dim = c(2, 3, 4)))
K <- cp_als(X, R = 2, maxiters = 20)
}

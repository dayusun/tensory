% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spgtr.R
\name{predict.spgtr}
\alias{predict.spgtr}
\title{Predict from a Tensor Regression Fit}
\usage{
\method{predict}{spgtr}(
  object,
  newX = NULL,
  newZ = NULL,
  type = c("response", "link", "class"),
  ...
)
}
\arguments{
\item{object}{A fit from \code{\link[=spgtr]{spgtr()}} or \code{\link[=spgtr_cv]{spgtr_cv()}}.}

\item{newX}{New subjects, in either form accepted by \code{\link[=spgtr]{spgtr()}}. If omitted,
predictions for the subjects used in fitting are returned.}

\item{newZ}{New ordinary covariates, required when the fit used \code{Z}.}

\item{type}{\code{"response"} (default) for the outcome scale -- a probability
for \code{binomial()}, an expected count for \code{poisson()}; \code{"link"} for the
linear predictor; \code{"class"} for a 0/1 label thresholded at \code{0.5}
(binomial fits only).}

\item{...}{Unused.}
}
\value{
A numeric vector with one prediction per subject.
}
\description{
Predict from a Tensor Regression Fit
}
\examples{
set.seed(3)
X <- lapply(1:80, function(i) matrix(rnorm(20), 5, 4))
y <- rbinom(80, 1, 0.5)
fit <- spgtr(X, y, u = c(1, 1))
predict(fit, X[1:5], type = "response")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ktensor_methods.R
\name{score}
\alias{score}
\alias{score.KTensor}
\title{Score the Similarity of Two Kruskal Tensors}
\usage{
score(x, ...)

\method{score}{KTensor}(x, y, lambda_penalty = TRUE, greedy = TRUE, ...)
}
\arguments{
\item{x}{A \code{KTensor} (the estimate).}

\item{...}{Additional arguments passed to methods.}

\item{y}{A \code{KTensor} with the same dimensions and number of components
(the reference).}

\item{lambda_penalty}{Logical; if \code{TRUE} (default) each pair score is
multiplied by \verb{1 - |la - lb| / max(la, lb)} computed from the normalized
weights.}

\item{greedy}{Logical; must be \code{TRUE} (greedy matching is the only
implemented strategy, as it is the MATLAB default).}
}
\value{
A list with elements \code{score} (mean of the matched component
scores), \code{perm} (the permutation of \code{y}'s components matched to \code{x}), and
\code{x} (a normalized copy of \code{x} arranged to match \code{y}).
}
\description{
Computes the factor match score between two \code{KTensor} objects with the
same number of components, mirroring the MATLAB Tensor Toolbox \code{score}.
Each candidate component pairing is scored by the product of the absolute
cosine similarities of the matched factor columns, optionally discounted by
the relative difference of the component weights; components are matched
greedily.
}

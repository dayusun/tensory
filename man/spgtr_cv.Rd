% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spgtr.R
\name{spgtr_cv}
\alias{spgtr_cv}
\title{Choose the Sparsity of a Tensor Regression by Cross-Validation}
\usage{
spgtr_cv(
  X,
  y,
  u = NULL,
  Z = NULL,
  family = stats::binomial(),
  lambda = NULL,
  nfolds = 5L,
  nlambda = 20L,
  lambda_ratio = 100,
  maxit = 500L,
  tol = 1e-08,
  ridge = 1e-08
)
}
\arguments{
\item{X, y, u, Z, family, maxit, tol, ridge}{As in \code{\link[=spgtr]{spgtr()}}.}

\item{lambda}{Optional vector of sparsity levels to try. Leave \code{NULL}
(default) to use an automatic log-spaced grid.}

\item{nfolds}{Number of cross-validation folds (default \code{5}).}

\item{nlambda, lambda_ratio}{Length of the automatic grid and the factor
between its smallest and largest value (defaults \code{20} and \code{100}).}
}
\value{
The \code{spgtr} fit at the selected sparsity, with three extra elements:
\code{lambda_min} (the chosen value), \code{lambda_seq} (the grid), and \code{cv} (a data
frame of \code{lambda} and mean out-of-fold \code{deviance}).
}
\description{
Runs \code{\link[=spgtr]{spgtr()}} over a range of sparsity levels, scores each one by
\code{nfolds}-fold cross-validation, and returns the model refitted at the best
value. Use this when you want the method to decide by itself how much of the
array to keep.
}
\details{
Folds are drawn at random once; within each fold the covariances, SIMPLS
bases, and adaptive weights are computed a single time and reused across the
whole \code{lambda} path, and each fit is warm-started from the previous (less
sparse) solution. Held-out fits are scored by the deviance of the chosen
\code{family}, which is the residual sum of squares for \code{gaussian()} and twice
the negative log-likelihood (up to a constant) otherwise; smaller is better.

If \code{lambda} is not supplied, the grid runs from \code{lambda_max / lambda_ratio}
up to \code{lambda_max}, the value at which the first proximal step would zero
every row of every factor matrix. That endpoint is a heuristic, so inspect
\code{fit$cv} and widen \code{lambda_ratio} if the selected value sits at either end
of the grid.
}
\examples{
set.seed(2)
B <- outer(c(2, rep(0, 5)), c(2, rep(0, 4)))
X <- lapply(1:100, function(i) matrix(rnorm(30), 6, 5))
eta <- vapply(X, function(xi) sum(B * xi), numeric(1))
y <- rbinom(100, 1, 1 / (1 + exp(-eta)))

fit <- spgtr_cv(X, y, u = c(1, 1), nfolds = 3, nlambda = 6)
fit$lambda_min
fit$cv
fit$selected # rows kept in each dimension
}
\seealso{
\code{\link[=spgtr]{spgtr()}}, \code{\link[=summary.spgtr]{summary.spgtr()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pqtr.R
\name{predict.pqtr}
\alias{predict.pqtr}
\title{Predict from a Partial Quantile Tensor Regression Fit}
\usage{
\method{predict}{pqtr}(object, newX = NULL, newZ = NULL, ...)
}
\arguments{
\item{object}{A fit from \code{\link[=pqtr]{pqtr()}} or \code{\link[=pqtr_cv]{pqtr_cv()}}.}

\item{newX}{New subjects, in either form accepted by \code{\link[=pqtr]{pqtr()}}. If omitted,
the fitted values for the subjects used in fitting are returned.}

\item{newZ}{New ordinary covariates, required when the fit used \code{Z}.}

\item{...}{Unused.}
}
\value{
A numeric vector of predicted conditional \code{tau}-th quantiles, one
per subject.
}
\description{
Predict from a Partial Quantile Tensor Regression Fit
}
\examples{
set.seed(3)
X <- lapply(1:80, function(i) matrix(rnorm(20), 5, 4))
y <- vapply(X, function(xi) xi[1, 1], numeric(1)) + rnorm(80)
fit <- pqtr(X, y, tau = 0.5, u = c(1, 1))
predict(fit, X[1:5])
}

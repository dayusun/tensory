% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cp_variants.R
\name{cp_wopt}
\alias{cp_wopt}
\title{Weighted CP Decomposition via Direct Optimization}
\usage{
cp_wopt(
  X,
  W,
  R,
  init = "random",
  maxiters = 500L,
  factr = 1e+07,
  printitn = 0L
)
}
\arguments{
\item{X}{A Tensor or array-like object (missing entries may hold any value,
typically 0).}

\item{W}{A weight tensor of the same dimensions as \code{X} (commonly 0/1).}

\item{R}{Target CP rank.}

\item{init}{\code{"random"} or a list of initial factor matrices.}

\item{maxiters}{Maximum optimizer iterations (default \code{500}).}

\item{factr}{\code{optim} L-BFGS-B \code{factr} convergence parameter.}

\item{printitn}{If positive, print the optimizer trace.}
}
\value{
A \code{KTensor}.
}
\description{
Fits a CP model to data with a weight (indicator) tensor by minimizing
\verb{||W * (X - K)||^2}, mirroring the MATLAB Tensor Toolbox \code{cp_wopt}. Use a
0/1 weight tensor to fit in the presence of missing entries.
}
\examples{
set.seed(1)
X <- tensor(array(rnorm(24), dim = c(2, 3, 4)))
W <- tensor(array(rbinom(24, 1, 0.8), dim = c(2, 3, 4)))
K <- cp_wopt(X, W, R = 2)
}

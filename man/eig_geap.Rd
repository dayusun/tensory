% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tensor_eigen.R
\name{eig_geap}
\alias{eig_geap}
\title{Generalized Eigenproblem Adaptive Power Method (GEAP)}
\usage{
eig_geap(
  A,
  B,
  maximize = TRUE,
  start = NULL,
  tol = 1e-10,
  maxiters = 500L,
  tau = 1e-06
)
}
\arguments{
\item{A}{A symmetric Tensor (all modes the same size).}

\item{B}{A symmetric positive definite Tensor of the same size and order.}

\item{maximize}{Logical; \code{TRUE} (default) seeks local maxima (\code{beta = 1}),
\code{FALSE} seeks local minima (\code{beta = -1}).}

\item{start}{Optional starting vector; random normal if omitted.}

\item{tol}{Convergence tolerance on the eigenvalue change (default
\code{1e-10}).}

\item{maxiters}{Maximum iterations (default \code{500}).}

\item{tau}{Positive-definiteness threshold for the adaptive shift
(default \code{1e-6}).}
}
\value{
A list with elements \code{lambda}, \code{x}, \code{converged}, \code{iterations}, and
\code{lambda_trace}.
}
\description{
Computes a generalized tensor eigenpair \verb{A x^(m-1) = lambda B x^(m-1)},
\verb{||x|| = 1}, for symmetric \code{A} and symmetric positive definite \code{B} using
the GEAP method of Kolda & Mayo (Algorithm 1), mirroring the MATLAB Tensor
Toolbox \code{eig_geap}. With \code{B = teneye(m, n)} this reduces to \code{\link[=eig_sshopm]{eig_sshopm()}}.
}
\examples{
set.seed(1)
A <- symmetrize(tensor(array(rnorm(81), dim = c(3, 3, 3, 3))))
res <- eig_geap(A, teneye(4, 3))
}
\references{
Kolda, T. G. and Mayo, J. R. (2014). An adaptive shifted power
method for computing generalized tensor eigenpairs. SIAM J. Matrix Anal.
Appl. 35(4), 1563-1581.
}

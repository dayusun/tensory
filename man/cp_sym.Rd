% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cp_sym.R
\name{cp_sym}
\alias{cp_sym}
\title{Symmetric CP Decomposition via Direct Optimization}
\usage{
cp_sym(
  X,
  R,
  init = "random",
  maxiters = 500L,
  factr = 1e+07,
  symmetrize = FALSE,
  printitn = 0L
)
}
\arguments{
\item{X}{A symmetric Tensor (all modes the same size).}

\item{R}{Number of symmetric rank-one components.}

\item{init}{\code{"random"} or an \verb{n x R} matrix of initial factors.}

\item{maxiters}{Maximum optimizer iterations (default \code{500}).}

\item{factr}{\code{optim} L-BFGS-B \code{factr} convergence parameter.}

\item{symmetrize}{Logical; if \code{TRUE}, symmetrize \code{X} first instead of
requiring exact symmetry.}

\item{printitn}{If positive, print the optimizer trace.}
}
\value{
A \code{SymKTensor}.
}
\description{
Fits a symmetric CP model \verb{sum_r lambda_r * u_r^(o m)} to a symmetric
tensor by minimizing the Frobenius residual over the weights and the shared
factor matrix with L-BFGS-B, mirroring the MATLAB Tensor Toolbox \code{cp_sym}.
}
\details{
With \verb{t_r = <X, u_r^(o m)>} and \verb{c_rs = u_r . u_s}, the objective is
\verb{||X||^2 - 2 sum_r lambda_r t_r + sum_rs lambda_r lambda_s c_rs^m}, whose
gradients are evaluated exactly using \code{\link[=ttsv]{ttsv()}}.
}
\examples{
set.seed(1)
u <- matrix(rnorm(6), 3, 2)
X <- as.tensor(symktensor(c(1, 2), u, m = 3))
S <- cp_sym(X, R = 2)
}

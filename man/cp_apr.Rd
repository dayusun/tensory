% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cp_variants.R
\name{cp_apr}
\alias{cp_apr}
\title{Poisson CP Decomposition (CP-APR) via Multiplicative Updates}
\usage{
cp_apr(
  X,
  R,
  tol = 1e-04,
  maxiters = 200L,
  maxinner = 10L,
  epsDivZero = 1e-10,
  init = "random",
  printitn = 0L
)
}
\arguments{
\item{X}{A nonnegative Tensor or array-like object (typically counts).}

\item{R}{Target CP rank.}

\item{tol}{KKT-violation stopping tolerance (default \code{1e-4}).}

\item{maxiters}{Maximum number of outer iterations (default \code{200}).}

\item{maxinner}{Maximum inner updates per mode per outer iteration
(default \code{10}).}

\item{epsDivZero}{Safeguard added before divisions (default \code{1e-10}).}

\item{init}{\code{"random"} or a list of initial nonnegative factor matrices.}

\item{printitn}{Print progress every \code{printitn} outer iterations.}
}
\value{
A \code{KTensor} with nonnegative factors and weights.
}
\description{
Fits a CP model to nonnegative (count) data by maximizing the Poisson
log-likelihood with the multiplicative-update algorithm of Chi & Kolda,
mirroring the MATLAB Tensor Toolbox \code{cp_apr} (\code{'mu'} method).
}
\examples{
set.seed(1)
X <- tensor(array(rpois(24, 3), dim = c(2, 3, 4)))
K <- cp_apr(X, R = 2, maxiters = 20)
}
\references{
Chi, E. C. and Kolda, T. G. (2012). On tensors, sparsity, and
nonnegative factorizations. SIAM J. Matrix Anal. Appl. 33(4).
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ktensor_methods.R
\name{normalize}
\alias{normalize}
\alias{normalize.KTensor}
\title{Normalize a Kruskal Tensor}
\usage{
normalize(x, ...)

\method{normalize}{KTensor}(x, mode = NULL, sort = FALSE, normtype = 2, ...)
}
\arguments{
\item{x}{A \code{KTensor}.}

\item{...}{Additional arguments passed to methods.}

\item{mode}{\code{NULL} (default) keeps all weight in \code{lambda}; a mode number
absorbs the weights into that factor matrix; \code{0} distributes the weights
evenly across all modes (each factor absorbs \code{lambda^(1/N)}).}

\item{sort}{Logical; if \code{TRUE}, sort the components by weight, descending.
Only allowed when the weights remain in \code{lambda} (i.e. \code{mode} is \code{NULL}).}

\item{normtype}{Norm to use for the factor columns (passed as the \code{p} in
the vector p-norm; default \code{2}).}
}
\value{
A normalized \code{KTensor}.
}
\description{
Normalizes the columns of every factor matrix, absorbing the magnitudes
into the weight vector \code{lambda}. Mirrors the MATLAB Tensor Toolbox
\code{normalize(K, N)} semantics.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cp_variants.R
\name{cp_arls}
\alias{cp_arls}
\title{CP Decomposition via Randomized (Sampled) ALS}
\usage{
cp_arls(
  X,
  R,
  tol = 1e-04,
  maxiters = 50L,
  nsamples = NULL,
  ridge = 1e-10,
  init = "random",
  printitn = 0L
)
}
\arguments{
\item{X}{A Tensor or array-like object.}

\item{R}{Target CP rank.}

\item{tol}{Convergence tolerance on change in (exact) fit.}

\item{maxiters}{Maximum number of sweeps (default \code{50}).}

\item{nsamples}{Number of sampled fibers per solve. Defaults to
\code{max(ceiling(10 * R * log2(R + 1)), 4 * R)} capped at the full count.}

\item{ridge}{Tikhonov regularizer added to the sampled normal equations
(default \code{1e-10}).}

\item{init}{\code{"random"} or a list of initial factor matrices.}

\item{printitn}{Print fit every \code{printitn} iterations.}
}
\value{
A \code{KTensor}.
}
\description{
Alternating least squares in which each subproblem is solved from a uniform
sample of tensor fibers rather than the full unfolding, in the spirit of
the MATLAB Tensor Toolbox \code{cp_arls}. Unlike \code{cp_arls}, no FFT-based mixing
is applied before sampling (a documented divergence); sampling is plain
uniform with replacement.
}
\examples{
set.seed(1)
X <- tensor(array(rnorm(60), dim = c(3, 4, 5)))
K <- cp_arls(X, R = 2, maxiters = 20)
}

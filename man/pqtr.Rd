% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pqtr.R
\name{pqtr}
\alias{pqtr}
\alias{coef.pqtr}
\alias{print.pqtr}
\title{Partial Quantile Tensor Regression (PQTR)}
\usage{
pqtr(
  X,
  y,
  tau = 0.5,
  Z = NULL,
  u = NULL,
  ridge = 1e-08,
  maxit = 200L,
  tol = 1e-08
)

\method{coef}{pqtr}(object, ...)

\method{print}{pqtr}(x, ...)
}
\arguments{
\item{X}{The tensor predictor, in either of two forms: a list of \code{n} equally
shaped \code{Tensor} objects, matrices, or arrays (one per subject), or a
single array/\code{Tensor} of order \code{m + 1} whose \strong{last} dimension indexes
the subjects.}

\item{y}{The outcome, a numeric vector with one value per subject.}

\item{tau}{The quantile to model, a single number strictly between 0 and 1.
\code{0.5} (default) is the median.}

\item{Z}{Optional \verb{n x q} matrix or data frame of ordinary (non-array)
covariates such as age or sex. These are never reduced.}

\item{u}{Number of directions kept per dimension of the array: an integer
vector of length \code{m}, or a single number used for every dimension. Leave
as \code{NULL} (default) to have each \code{u[k]} chosen by the eigenvalue-ratio
rule of the reference implementation.}

\item{ridge}{Relative floor applied to covariance eigenvalues for numerical
stability (default \code{1e-8}).}

\item{maxit, tol}{Iteration cap and convergence tolerance of the quantile
regression solver.}

\item{object}{A fit from \code{\link[=pqtr]{pqtr()}} or \code{\link[=pqtr_cv]{pqtr_cv()}}.}

\item{...}{Unused.}

\item{x}{A fit from \code{\link[=pqtr]{pqtr()}} or \code{\link[=pqtr_cv]{pqtr_cv()}}.}
}
\value{
An object of class \code{pqtr}, a list whose most useful elements are:
\describe{
\item{\code{coef}}{coefficient array as a \link{TTensor} with core \code{D} and weight
matrices \code{W}; \code{coef(fit)} returns it and \code{as.tensor(coef(fit))}
expands it to a dense \link{Tensor}.}
\item{\code{alpha}, \code{gamma}}{intercept and coefficients of \code{Z}, on the
centered scale.}
\item{\code{u}, \code{W}, \code{core}, \code{scores}}{reduced dimensions, weight matrices,
latent coefficients, and the \verb{n x prod(u)} matrix of latent scores.}
\item{\code{fitted}, \code{residuals}, \code{loss}}{fitted conditional quantiles, their
residuals, and the attained check loss.}
}
}
\description{
Fits a quantile regression in which each subject contributes a whole array
of measurements -- an image, a connectivity matrix, a space-by-time grid --
together with an optional handful of ordinary covariates. Instead of the
mean of the outcome, \code{pqtr()} models a chosen quantile of it, so it answers
questions such as "which parts of the image go with an unusually \emph{low}
score?" rather than only "which parts go with the average score?".
}
\details{
Ordinary quantile regression cannot be run on the flattened array, because
one array holds far more numbers than there are subjects. \code{pqtr()} solves
that with a partial-least-squares construction: each dimension of the array
is compressed to a few directions chosen for their association with the
quantile of interest, the quantile regression is fit on those, and the
answer is mapped back to the original array shape.
}
\section{What you get back}{

A coefficient array of exactly the same shape as one subject's data,
available through \code{coef(fit)}. A large positive entry means "a high value at
this position pushes the \code{tau}-th quantile of the outcome up". Unlike
\code{\link[=spgtr]{spgtr()}} there is no sparsity penalty, so every entry is non-zero; read the
array by magnitude rather than by which entries are exactly zero.
}

\section{How to use it (short version)}{

\enumerate{
\item Put your data in a list: \code{X[[i]]} is subject \code{i}'s matrix or array, all
the same shape, and \code{y} is a numeric vector with one value per subject.
\item Run \code{fit <- pqtr(X, y, tau = 0.5)} for the median, or another \code{tau} in
\verb{(0, 1)} for a different part of the outcome distribution.
\item Fit several quantiles and compare the coefficient arrays: if they differ,
the array affects the spread or shape of the outcome, not just its
centre.
\item Use \code{\link[=pqtr_cv]{pqtr_cv()}} if you would rather have the number of directions chosen
by cross-validation than by the built-in eigenvalue-ratio rule.
}
}

\section{How it works (technical)}{

Writing \code{Q(tau)} for the fitted \code{tau}-th quantile of the outcome given the
covariates \code{Z} alone, the outcome enters only through the working residual
\verb{tau - 1\{y < Q(tau)\}}, the subgradient of the check loss at the
covariate-only fit. Let \code{C} be the cross-covariance tensor between the
centered predictor and that residual, and \code{Sigma_k} the mode-\code{k} marginal
covariance of the predictor. The mode-\code{k} signal matrix is
\verb{U_k = C_(k) (kron_\{j != k\} Sigma_j^-1) C_(k)'}, and the weight matrix
\code{W_k} collects \code{u[k]} directions obtained by repeatedly taking the leading
eigenvector of \code{U_k} and deflating with the oblique projector
\verb{I - Sigma_k W (W' Sigma_k W)^-1 W'}. That projector leaves \code{W_k} with
orthonormal columns, which is the deflation used in the reference MATLAB
implementation; it differs from the SIMPLS deflation in \code{\link[=tepls]{tepls()}} and
\code{\link[=spgtr]{spgtr()}}, which instead makes the latent scores uncorrelated. The two agree
whenever \code{U_k} has rank one and span different subspaces otherwise.

The predictor is then reduced to latent scores
\verb{T_i = X_i x_1 W_1' ... x_m W_m'}, the linear quantile regression of \code{y} on
\verb{(Z, vec(T))} is fit, and the coefficient array is reconstructed as
\verb{B = D x_1 W_1 ... x_m W_m} from the latent coefficients \code{D}. Nothing in
this chain inverts a \verb{prod(p) x prod(p)} matrix, which is what makes the
method scale to large arrays.
}

\section{Divergences from the reference implementation}{

Ported from the MATLAB code accompanying Sun et al. (2024)
(\url{https://github.com/dayusun/PQTR}), with four deliberate differences.
(1) The inner quantile regressions are solved by the MM algorithm of Hunter
& Lange (2000) rather than by MATLAB's \code{fminunc}, which is a smooth solver
applied to a non-smooth objective; the MM surrogate is exactly matched to
the check loss. (2) The predictor is centered before the latent scores are
formed, so \code{alpha} is the intercept at the training-sample means; the
coefficient array \code{B} is unchanged by this. (3) Cross-validated selection of
the reduced dimension lives in \code{\link[=pqtr_cv]{pqtr_cv()}}, which takes an explicit grid of
candidate dimensions instead of enumerating every combination. (4) The
eigenvalue-ratio rule searches at most five candidate dimensions per mode,
rather than \code{sqrt(n - q)} of them: past the rank of the signal matrix the
eigenvalues are noise, and the largest ratio among them otherwise wins and
returns a dimension far too big to estimate. This matches the rule used by
\code{\link[=spgtr]{spgtr()}} and \code{\link[=tepls]{tepls()}}.
}

\examples{
# 150 subjects measured on an 8 x 6 grid; only the top-left corner drives
# the outcome, and it does so more strongly in the upper tail.
set.seed(1)
B <- outer(c(1.5, rep(0, 7)), c(1, rep(0, 5)))
X <- lapply(1:150, function(i) matrix(rnorm(48), 8, 6))
signal <- vapply(X, function(xi) sum(B * xi), numeric(1))
y <- signal + (1 + 0.6 * signal) * rnorm(150)

med <- pqtr(X, y, tau = 0.5)
upper <- pqtr(X, y, tau = 0.9)
med

# Coefficient arrays, same shape as one subject's data.
round(as.tensor(coef(med))$as_array()[1:3, 1:3], 2)
round(as.tensor(coef(upper))$as_array()[1:3, 1:3], 2)

# Predicted conditional quantiles for new subjects.
head(predict(med, X[1:5]))
}
\references{
Sun, D., Zhang, X. and Zhang, S. (2024). Partial quantile tensor regression.
Journal of the American Statistical Association 120(551).

Hunter, D. R. and Lange, K. (2000). Quantile regression via an MM algorithm.
Journal of Computational and Graphical Statistics 9(1), 60-77.
}
\seealso{
\code{\link[=pqtr_cv]{pqtr_cv()}} to choose \code{u} by cross-validation, \code{\link[=predict.pqtr]{predict.pqtr()}},
and \code{\link[=spgtr]{spgtr()}} / \code{\link[=tepls]{tepls()}} for the mean-regression counterparts.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cp_sym.R
\name{tucker_sym}
\alias{tucker_sym}
\title{Symmetric Tucker Decomposition}
\usage{
tucker_sym(
  X,
  r,
  tol = 1e-06,
  maxiters = 100L,
  init = "nvecs",
  symmetrize = FALSE
)
}
\arguments{
\item{X}{A symmetric Tensor (all modes the same size).}

\item{r}{Target subspace rank.}

\item{tol}{Convergence tolerance on change in core norm (default \code{1e-6}).}

\item{maxiters}{Maximum number of iterations (default \code{100}).}

\item{init}{\code{"nvecs"} (default) or an initial \verb{n x r} matrix with
orthonormal columns.}

\item{symmetrize}{Logical; if \code{TRUE}, symmetrize \code{X} first.}
}
\value{
A \code{TTensor} whose factor matrices are all identical.
}
\description{
Computes a Tucker decomposition with a single orthonormal factor matrix
shared by every mode via higher-order orthogonal iteration on the
symmetric subspace, mirroring the MATLAB Tensor Toolbox \code{tucker_sym}.
}
\examples{
set.seed(1)
X <- symmetrize(tensor(array(rnorm(27), dim = c(3, 3, 3))))
T <- tucker_sym(X, r = 2)
}

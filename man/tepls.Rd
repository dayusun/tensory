% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tepls.R
\name{tepls}
\alias{tepls}
\title{Tensor Envelope Partial Least Squares Regression (TEPLS)}
\usage{
tepls(X, Y, u, ridge = 1e-08)
}
\arguments{
\item{X}{Tensor predictor: a list of observations, or an order-\code{(m + 1)}
tensor with observations in the last mode.}

\item{Y}{Response: numeric vector (length \code{n}) or \verb{n x r} matrix.}

\item{u}{Envelope dimension per mode: an integer vector of length \code{m}, or a
scalar recycled across modes. Each \code{u[k]} must satisfy
\verb{1 <= u[k] <= p_k}.}

\item{ridge}{Small ridge added when inverting covariance / normal-equation
matrices for numerical stability (default \code{1e-8}).}
}
\value{
An object of class \code{tepls}: a list with elements \code{coef} (the
coefficient \code{Tensor}, order \code{m} for scalar response or order \code{m + 1} with
a trailing response mode otherwise), \code{W} (list of factor matrices),
\code{intercept}, \code{Xbar}, \code{dims}, \code{u}, \code{r}, and \code{fitted}.
}
\description{
Fits the tensor-predictor partial least squares regression of Zhang & Li
(2017). A tensor predictor \code{X} (order \code{m}) is reduced to a low-dimensional
latent tensor via per-mode envelope factor matrices estimated with a SIMPLS
iteration (their Algorithm 4), and the response is regressed on the latent
tensor. The coefficient tensor is reconstructed in the original predictor
space (their Lemma 2), giving the \code{B_PLS} estimator.
}
\details{
The predictor may be supplied either as a list of \code{n} \code{Tensor}/array
observations that share the same dimensions, or as a single
order-\code{(m + 1)} \code{Tensor}/array whose \strong{last} mode indexes the \code{n}
observations. The response \code{Y} is a length-\code{n} numeric vector (scalar
response) or an \verb{n x r} matrix (multivariate response).

Each mode's factor matrix \code{W_k} has \code{u[k]} columns; \code{u} is the envelope
dimension per mode (recycled if a scalar). The mode-\code{k} marginal covariance
uses the moment estimator (their eq. 1), the mode-\code{k} cross-covariance is
standardized as in Algorithm 4, and the reduced regression of \code{Y} on the
latent tensor is fit by (regularized) least squares (their Step 6).

The mode-\code{k} second-moment matrix maximized in the SIMPLS step,
\verb{C_(k) (Sigma_Y^-1 kron ... kron Sigma_1^-1) C_(k)^T} (excluding
\code{Sigma_k}), is identical to the \verb{U U^T} matrix in the reference
implementation \code{TEReg::TensPLS_fit}. Two deliberate divergences: (1) that
package estimates each mode's basis with an envelope (\code{EnvMU}) optimizer,
whereas this function uses the SIMPLS deflation of Algorithm 4 exactly;
(2) the estimated factor directions and the reduced-regression fit are both
invariant to the per-mode scale ambiguity of the separable covariance,
avoiding the need to pin the Kronecker scale.
}
\examples{
set.seed(1)
p <- c(8, 6)
B <- outer(c(1, rep(0, 7)), c(1, rep(0, 5))) # rank-1, envelope dim 1 per mode
X <- lapply(1:80, function(i) matrix(rnorm(prod(p)), p[1], p[2]))
y <- vapply(X, function(xi) sum(B * xi), numeric(1)) + rnorm(80, sd = 0.1)
fit <- tepls(X, y, u = c(1, 1))
}
\references{
Zhang, X. and Li, L. (2017). Tensor envelope partial
least-squares regression. Technometrics 59(4), 426-436.
}
\seealso{
\code{\link[=predict.tepls]{predict.tepls()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tucker_decomposition.R
\name{hosvd}
\alias{hosvd}
\title{Higher-Order Singular Value Decomposition}
\usage{
hosvd(
  X,
  ranks = NULL,
  tol = NULL,
  dimorder = NULL,
  sequential = TRUE,
  verbosity = 0L
)
}
\arguments{
\item{X}{A Tensor or array-like object.}

\item{ranks}{Optional integer vector of per-mode truncation ranks. Scalar
inputs are replicated across modes.}

\item{tol}{Relative truncation tolerance. Ignored when \code{ranks} is supplied.
Per-mode truncation keeps enough singular vectors to ensure the dropped
energy is below \code{tol^2 / ndims(X)} of \code{fnorm(X)^2}.}

\item{dimorder}{Order in which modes are processed (matters only for
\code{sequential = TRUE}).}

\item{sequential}{Logical; if \code{TRUE} (default) the ST-HOSVD variant is used,
progressively contracting the core, which is typically more accurate and
cheaper than the classical HOSVD.}

\item{verbosity}{Non-negative integer controlling diagnostic messages.}
}
\value{
A \code{TTensor}.
}
\description{
Computes the (truncated) higher-order SVD of a tensor, returning a Tucker
representation whose factor matrices span the leading left singular spaces
of the mode-n unfoldings. Mirrors the MATLAB Tensor Toolbox \code{hosvd}.
}
\examples{
X <- tensor(array(runif(60), dim = c(3, 4, 5)))
T <- hosvd(X, ranks = c(2, 3, 4))
}

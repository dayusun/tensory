# Mask Tensor Values

Extracts values from a tensor at positions corresponding to nonzero
entries of a mask tensor.

## Usage

``` r
mask(x, w, ...)
```

## Arguments

- x:

  A Tensor object.

- w:

  A Tensor-like mask.

- ...:

  Additional arguments passed to methods.

## Value

A vector of extracted values.
